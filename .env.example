# AuraForce Environment Variables Example
#
# Copy this file to .env.local and fill in your actual values
# Never commit .env or .env.local to version control

# ============================================
# Database Configuration
# ============================================
# MySQL connection string for Prisma ORM
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: mysql://root:password@localhost:3306/auraforce
DATABASE_URL="mysql://user:password@localhost:3306/auraforce"

# ============================================
# Auth.js v5 Configuration
# ============================================
# REQUIRED: Secret key for session encryption
# Generate a secure key with: openssl rand -base64 32
# This is CRITICAL for security - use a different value in production
AUTH_SECRET="generate-with-openssl-rand-base64-32"

# OPTIONAL: The base URL of your application
# Defaults to: http://localhost:3000 in development
# Required in production if your app is not hosted at root
# Example: https://yourdomain.com
AUTH_URL="http://localhost:3000"

# ============================================
# Email/SMTP Configuration (Epic 2, Story 2.4)
# ============================================
# SMTP server configuration for sending emails
# Used for email verification and password reset

# REQUIRED: SMTP server host
# Examples:
# - Gmail: smtp.gmail.com
# - Outlook: smtp-mail.outlook.com
# - QQ: smtp.qq.com
# - 163: smtp.163.com
SMTP_HOST="your-smtp-host"

# SMTP server port
# Common ports: 25, 465 (SSL), 587 (TLS)
SMTP_PORT="587"

# SMTP username (usually the email address)
# Example: noreply@yourdomain.com
SMTP_USER="your-smtp-username"

# SMTP password or app-specific password
# For Gmail, you need to generate an app password:
# https://support.google.com/accounts/answer/185833
SMTP_PASS="your-smtp-password"

# From email address (email will appear to come from this)
# Example: noreply@auraforce.com
SMTP_FROM="noreply@auraforce.com"

# Application public URL (used in email links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ============================================
# Claude Agent SDK Configuration (Epic 1, Story 1.7)
# ============================================
# Claude API key for Agent SDK
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY="your-claude-api-key-here"

# ============================================
# Application Configuration
# ============================================
# Node environment: development, staging, or production
NODE_ENV="development"

# ============================================
# Optional: OAuth Provider Configuration (Epic 2+)
# ============================================
# GitHub OAuth (example - add in Epic 2 when implementing OAuth)
# AUTH_GITHUB_ID="your-github-client-id"
# AUTH_GITHUB_SECRET="your-github-client-secret"

# Google OAuth (example - add in Epic 2 when implementing OAuth)
# AUTH_GOOGLE_ID="your-google-client-id"
# AUTH_GOOGLE_SECRET="your-google-client-secret"

